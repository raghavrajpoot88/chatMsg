import { Injectable } from '@angular/core';
import { userInfo } from '../registration/model/userInfo';

@Injectable({
  providedIn: 'root'
})
export class RegisterService {

  constructor() { }
  userRegistration(data: userInfo){
    
  }
}
